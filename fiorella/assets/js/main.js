const images = document.querySelectorAll("flwr")
const menu = document.querySelectorAll(".menu .select")
const atribute = div.getAttribute('atribute');
function selectFlowers(){
    if(!(div.getAttribute('class').includes('atribute'))){
        div.style.display='block'
    }
    else{
        div.style.display='none'
    }
}
